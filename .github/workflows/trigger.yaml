name: Trigger sync

on:
  repository_dispatch:
    types: [main_updated]

jobs:
  trigger-sync:
    runs-on: ubuntu-latest
    if: github.repository == 'inji0212/CICD_copied'
    steps:
      - run : echo 'sync'
